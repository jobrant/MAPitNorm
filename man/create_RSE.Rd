% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_RSE.R
\name{create_RSE}
\alias{create_RSE}
\title{Create a RangedSummarizedExperiment from Methylation list of list of dataframes containing methylation data}
\usage{
create_RSE(lol, groups = NULL, exp_metadata = list(), verbose = TRUE)
}
\arguments{
\item{lol}{A \strong{doubly-nested named list}.
\itemize{
\item \bold{Tier 1 (Names):} Group identifiers (e.g., "Group1", "Group2").
\item \bold{Tier 2 (Names):} Sample identifiers (e.g., "Rep1", "Rep2").
\item \bold{Elements:} Data frames or data.tables containing at least
\code{chr, pos, strand, site, uniqueID, cov}, and \code{rate}.
}}

\item{groups}{Groups to include in the summarized experiment object. If not specified, all groups are retained.}

\item{exp_metadata}{Any additional list containing experiment-level metadata to be stored in the
\code{metadata} slot of the SummarizedExperiment.}

\item{verbose}{Logical. If TRUE (default), prints progress messages during processing.}
}
\value{
A \code{RangedSummarizedExperiment} object with two assays: \code{coverage}
and \code{rate}. Site-level genomic metadata is preserved in the
\code{rowRanges} slot as a \code{GRanges} object.
}
\description{
This function takes a nested named list of methylation data frames and converts them
into a single \code{RangedSummarizedExperiment} object. It expects all data frames to
contain filtered and common sites across all samples.
}
